#include<stdio.h>
		bool kiem_tra_so_lon_nhat(int max){
			
				int a[n]=0;
			for (int i = 0; i < n; ++i){
				if( max < a[n]){
					return true;
				}
				return false;
			}
	
		}
	int	main(){
		int n;
		printf("nhap kieu \n",);
		scanf("%d",n)

		int 	a[n];
		for(int i=0;i<n;i++){
		printf("Nhap phan tu:\n");
		scanf("%d",&a[i]);
	}
		if(kiem_tra_so_lon_nhat(a[i]));
			printf("so lon nhat la %d \n", );

	return	0;
	}