#include <stdio.h>

int main(){
	float a,b;
	printf("nhap so a=\n");
	scanf("%f",&a);

    printf("nhap so b=\n",b);
    scanf(%f,&b);
	if(b!=0){
		if(a>b)  {
		printf("thuong 2 so la %f", a/b );
	}else{
		printf("tich 2 so la %f", a*b );
	}
    }
    else
    {
        printf(khong thoa man dieu kien cua b , moi nhap lai");
     {
	return 0;
}

     //hoang trong ha 