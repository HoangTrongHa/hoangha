#include<stdio.h>

	bool kiem_tra_so_lon_nhat( int x){

	for (int i = 0; i < 5 ; ++i){
		 int max=0;
		 int a[i];
		if(max == a[i]){
			x++;
		}
	 }
	 if(x == max){
	 	return true;

	 	}
	 	return false;
	}

  int	main(){
  		int a[5]={1,2,3,4,5};
  		
        if(kiem_tra_so_lon_nhat(x))
    }
    printf("Gia tri max  la %d",max);

  	return	0;
  	}